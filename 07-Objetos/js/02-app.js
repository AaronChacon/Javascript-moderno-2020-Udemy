const product = {
    //llave:valor
    nombre: "Monitor de 20 pulgadas",
    precio: 300,
    disponible: true,
}

// se puede acceder mediante la sintaxis de puntos

console.log(product.nombre);
console.log(product.disponible);

// No muy utilizado pero se puede acceder mediante corchetes []

console.log(product['nombre']);